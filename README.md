# OpenRisk Plugin Registry

This repository is the official registry of plugins for the [OpenRisk](https://github.com/OpenRiskPlatform) platform.

Each plugin lives in its own directory and must contain a `plugin.json` manifest. The root `plugins.json` index is generated automatically on every commit to `main`.
